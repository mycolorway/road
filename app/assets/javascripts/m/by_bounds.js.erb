(function($) {

var pageId = 'page-by-bounds',
	page = '#' + pageId;

$(function() {
	


});

function initMap (argument) {
	// map = new BMap.Map('#map-container', rd.mapBasicOpts);
	// // map.addEventlistener('load', function() {

	// // })
	// centerAndZoom('成都', '15');

	// mapNav = Bmap.Nav
}

})(jQuery);
